---
applyTo: '**'
---

There is a file called `sysext.sh` that will build and deploy a sysext to a VM that you can connect to by running `ssh fernie@192.168.100.129`. If you need sudo, instead do `ssh root@192.168.100.129`.